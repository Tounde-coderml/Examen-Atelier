version: '3.8'

services:
  mysql:
    image: mysql:8.0
    container_name: atelier_mysql
    environment:
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD:-password}
      MYSQL_DATABASE: ${DB_DATABASE:-Examen_atelier}
      MYSQL_USER: ${DB_USERNAME:-atelier}
      MYSQL_PASSWORD: ${DB_PASSWORD:-password}
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      timeout: 20s
      retries: 10
    networks:
      - atelier_network

  backend:
    build:
      context: .
      dockerfile: Dockerfile.backend
    container_name: atelier_backend
    environment:
      DB_CONNECTION: mysql
      DB_HOST: mysql
      DB_PORT: 3306
      DB_DATABASE: ${DB_DATABASE:-Examen_atelier}
      DB_USERNAME: ${DB_USERNAME:-atelier}
      DB_PASSWORD: ${DB_PASSWORD:-password}
      APP_URL: http://localhost:8000
      APP_DEBUG: false
    depends_on:
      mysql:
        condition: service_healthy
    ports:
      - "8000:8000"
    volumes:
      - .:/app
    networks:
      - atelier_network

  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    container_name: atelier_frontend
    environment:
      VITE_API_URL: http://localhost:8000/api
    ports:
      - "5173:5173"
    depends_on:
      - backend
    networks:
      - atelier_network

volumes:
  mysql_data:

networks:
  atelier_network:
    driver: bridge
